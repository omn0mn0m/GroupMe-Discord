version: '3'

services:
  groupme-discord-bot:
    container_name: groupme-discord-bot
    build: .
    command: python bot/client.py
    volumes:
      - ./bot:/app/bot
    ports:
      - "8088:8088"
    env_file: .env

